// 0~1 정규화 좌표 (오른발 기준) — 화면 좌표: (0,0)=좌상단, y는 아래로 증가
export type SensorPt = { id: number; x: number; y: number };

// 👇 이미지(발끝 위 / 뒤꿈치 아래) 배치에 맞춘 초기 좌표
// 필요하면 0.02 단위로 미세 조정하세요.
export const RIGHT_LAYOUT_20: SensorPt[] = [
  // 발끝(위쪽)
  { id:9, x:0.35, y:0.08 }, { id:10,  x:0.57, y:0.10 },
  { id:8, x:0.28, y:0.24 }, { id:12, x:0.50, y:0.24 }, { id:20,  x:0.72, y:0.23 },
  { id:7, x:0.27, y:0.41 }, { id:16, x:0.50, y:0.41 }, { id:19,  x:0.73, y:0.41 },

  // 스트랩 위/아래 두 줄
  { id:2, x:0.35, y:0.57 }, { id:15, x:0.50, y:0.57 }, { id:18, x:0.67, y:0.57 },
  { id:3, x:0.35, y:0.74 }, { id:13, x:0.50, y:0.75 }, { id:17, x:0.65, y:0.75 },

  // 뒤꿈치(아래쪽)
  { id:5,  x:0.40, y:0.90 }, { id:4,  x:0.57, y:0.90 },

];

// ──────────────────────────────────────────────────────────────────────────────
// C0~C15 → 센서 ID 매핑 (하드웨어 기준 확정본)
// ──────────────────────────────────────────────────────────────────────────────
export const CHANNEL_TO_SENSOR: Record<number, number> = {
  0: 2,  // C0
  1: 3,  // C1
  2: 4,  // C2
  3: 5,  // C3
  4: 7,  // C4
  5: 8,  // C5
  6: 9,  // C6
  7:  10,  // C7
  8: 12,  // C8
  9:  13,  // C9
 10: 15,  // C10
 11:  16,  // C11
 12:  17,  // C12
 13:  18,  // C13
 14: 19,  // C14
 15: 20,  // C15
};

// ──────────────────────────────────────────────────────────────────────────────
// 역매핑: 센서 ID → 채널 (디버깅 또는 역참조용)
// ──────────────────────────────────────────────────────────────────────────────
export const SENSOR_TO_CHANNEL: Record<number, number> = Object.fromEntries(
  Object.entries(CHANNEL_TO_SENSOR).map(([ch, sid]) => [sid, Number(ch)])
);
